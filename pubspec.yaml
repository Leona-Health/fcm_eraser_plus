name: fcm_eraser_plus
description: "This plugin helps manage notifications when users interact with them or when they are no longer needed."
version: 1.1.1
homepage: https://github.com/AnhPhon/fcm_eraser_plus

environment:
  sdk: ^3.3.0
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_lints: any

  flutter_test:
    sdk: flutter

flutter:

  plugin:
    platforms:
      android:
        package: eraser.fcm.plus.fcm_eraser_plus
        pluginClass: FcmEraserPlusPlugin
      ios:
        pluginClass: FcmEraserPlusPlugin